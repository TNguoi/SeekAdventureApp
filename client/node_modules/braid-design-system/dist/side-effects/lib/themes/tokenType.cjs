"use strict";
const extractFontMetricsForTheme = ({
  capHeight,
  ascent,
  descent,
  lineGap,
  unitsPerEm
}) => ({
  capHeight,
  ascent,
  descent,
  lineGap,
  unitsPerEm
});
exports.extractFontMetricsForTheme = extractFontMetricsForTheme;
