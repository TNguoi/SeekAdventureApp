const extractFontMetricsForTheme = ({
  capHeight,
  ascent,
  descent,
  lineGap,
  unitsPerEm
}) => ({
  capHeight,
  ascent,
  descent,
  lineGap,
  unitsPerEm
});
export {
  extractFontMetricsForTheme
};
